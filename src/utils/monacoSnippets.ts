import type * as monaco from 'monaco-editor';

export function registerSnippets(_editor: monaco.editor.IStandaloneCodeEditor): void {
  // Snippet registration is handled by Monaco's built-in completion providers
}
